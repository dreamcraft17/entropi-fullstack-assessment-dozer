import { PlaceholderSection } from "../PlaceholderSection";

export default function ProjectsPage() {
  return (
    <PlaceholderSection
      title="Projects"
      icon="📁"
      description="Idea for grouping links into a few projects or campaigns."
      features={[
        "Create a few \"collections\" of links",
        "Separate work, personal, and campaign links",
        "Potential to show per‑project stats later",
      ]}
    />
  );
}
