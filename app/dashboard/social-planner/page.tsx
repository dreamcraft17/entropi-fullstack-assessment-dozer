import { PlaceholderSection } from "../PlaceholderSection";

export default function SocialPlannerPage() {
  return (
    <PlaceholderSection
      title="Social planner"
      icon="🗓️"
      description="Initial idea: a simple content calendar that ties back to links in the bio."
      features={[
        "List planned posts with dates",
        "Note which link will be promoted in each post",
      ]}
    />
  );
}

