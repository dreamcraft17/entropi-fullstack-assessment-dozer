import { PlaceholderSection } from "../PlaceholderSection";

export default function InstagramAutoReplyPage() {
  return (
    <PlaceholderSection
      title="Instagram auto-reply"
      icon="💬"
      description="Name borrowed from the Linktree reference: the idea is a DM auto‑reply that sends a specific link."
      features={[
        "Configure a short auto‑reply message",
        "Attach one of the existing bio links",
      ]}
    />
  );
}

